#!/usr/bin/env sh
: "${R_HOME=`R RHOME`}"

# Source common configuration logic
. ./configure_common.sh

# Setup vendor directory
setup_vendor_directory

# Run the R configuration script
"${R_HOME}/bin/Rscript" tools/config.R
