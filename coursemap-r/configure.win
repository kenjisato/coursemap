#!/usr/bin/env sh

# Source common configuration logic
. ./configure_common.sh

# Setup vendor directory
setup_vendor_directory

# Run the R configuration script (Windows version)
"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" tools/config.R
