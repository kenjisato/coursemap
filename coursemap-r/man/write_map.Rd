% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coursemap.R
\name{write_map}
\alias{write_map}
\title{Save a course map to file}
\usage{
write_map(x, filename, format = NULL, width = 10, height = 8, ...)
}
\arguments{
\item{x}{Course map object to save}

\item{filename}{File name to create on disk}

\item{format}{Output format: "svg", "png", or "dot" (auto-detected from filename if NULL)}

\item{width}{Width in inches (for future use, currently ignored)}

\item{height}{Height in inches (for future use, currently ignored)}

\item{...}{Additional arguments (ignored)}
}
\value{
Path to the saved file (invisibly)
}
\description{
Save a course map object to a file in various formats.
}
\examples{
\dontrun{
cm <- coursemap("./courses")

# Save as PNG (format auto-detected)
write_map(cm, "course_map.png")

# Save as SVG with explicit format
write_map(cm, "course_map", format = "svg")

# Save as DOT format
write_map(cm, "course_map.dot")
}

}
